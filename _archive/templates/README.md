# Archived static templates

정적(크몽) 트랙 작업물 보관. **삭제 아님.**

활성 작업은 WordPress (`wordpress/{slug}/`) 기준.
문법 참고용 `templates/cafe24_shop/` 은 아카이브하지 않고 `templates/`에 유지.

복구 시: 이 폴더 → `templates/{slug}/` 로 다시 옮기면 됨.
