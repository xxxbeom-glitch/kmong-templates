---
description: 정적 납품 규칙 (동결 — WP는 70-wordpress)
alwaysApply: false
globs:
  - _archive/delivery/**
  - _harness/package-delivery.js
---

# Delivery (정적 · 동결)

> **활성 WP 납품:** `70-wordpress.mdc` · `wordpress/{slug}/dist/{slug}.zip`  
> 본 규칙은 구 정적·아임웹 납품(`_archive/delivery/`) 참고용.

- 구 경로 `_delivery/{slug}/` → 보관: `_archive/delivery/`
- 정적 패키징 스크립트 `package-delivery.js` — **신규 미사용**
- 루트 `_delivery/` · `_delivery-wp/` **재생성 금지**

금지:
- 플랫폼별 하위 폴더 (`imweb`, `cafe24` 등) 루트에 재도입
- 정적 납품을 활성 WP 워크플로로 사용
