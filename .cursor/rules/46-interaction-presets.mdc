---
description: PC 인터랙션 프리셋 카탈로그·선택·승인 (구현은 45-interaction-patterns)
alwaysApply: false
globs:
  - templates/**/*
  - wordpress/**/*
  - .cursor/rules/45-interaction-patterns.mdc
  - _docs/interaction-presets-guide.md
  - _logs/decision-log.md
---

# Interaction Presets (PC)

> **구현 방식:** `.cursor/rules/45-interaction-patterns.mdc` (jQuery·hover/터치·타이밍)  
> **사람용 요약:** `_docs/interaction-presets-guide.md` (카탈로그·한글명만 — **템플릿별 연결 현황은 `_logs/decision-log.md`**)  

## 목적 · 범위

- 템플릿 제작 시 **반복 인터랙션**을 preset ID로 표준화한다.
- **카탈로그 10종 전부 등록** — 템플릿·섹션에 **연결(적용)은 사용자가 선택·승인**할 때만 한다.
- **현재 범위: PC (@1920 desktop)** — 모바일·터치 대체는 `35-responsive.mdc` + `45-interaction-patterns.mdc`.
- **GNB 2depth 패턴 2종** (`gnb-full-expand` · `gnb-item-panel`) — header 전용 · 표준 preset 10종과 **별도** · 아래 「GNB 패턴」.
- **실제 JS/CSS 구현 코드는 이 파일에 두지 않는다.** (템플릿 `main.js` / 섹션 CSS에만)

## 전역 규칙

- 사용자가 **명시·승인하지 않은 preset**은 임의로 추가하지 않는다.
- **한 섹션**에 preset **과다 적용 금지** — 아래 「섹션당 상한」 참고.
- 인터랙션은 **콘텐츠 전달을 방해**하면 안 된다 (가독성·스크롤·포커스 우선).
- **중복 애니메이션** 금지 — 동일 요소에 scroll-reveal + hover-scale 등 겹치지 않음.
- preset **ID(slug)는 프로젝트 전역 동일** — 아래 표기만 사용, **한글명은 명령·대화용**(decision-log·QA는 ID 기록).
- 사용자 **한글명** 또는 **ID** 지정 모두 승인으로 인정 — AI는 해당 preset 1개로 매핑.
- Figma에 motion이 없어도, **사용자가 preset ID를 섹션에 지정·승인**하면 적용 가능 (`decision-log` · `_docs/context-guide.md`).

## 섹션당 상한 (권장)

| 유형 | preset | 섹션당 권장 |
|------|--------|-------------|
| 진입·스크롤 | `scroll-reveal`, `typing-text` | **1개** |
| 슬라이드·드래그 | `hero-progress-slider`, `drag-scroll` | **1개** |
| hover 계열 | `hover-tone`, `image-scale-hover`, `grayscale-hover-color`, `button-text-slide-hover`, `expand-card-hover` | **합계 2개 이하** |
| 숫자 | `stats-counter` | **1개** (stats 섹션 등) |
| **GNB 2depth** | `gnb-full-expand`, `gnb-item-panel` | **템플릿당 1개** (header 전용 · 표준 preset 상한 **미포함**) |

초과 시 **사용자 승인** 없이 적용 금지.

## 적용 절차 (섹션 구현 시)

```
Figma MCP 분석
→ (인터랙션 검토 시) preset 후보 ID 제안 · 섹션당 상한 확인
→ 사용자 preset ID 선택·승인  ← 카탈로그에 있어도 자동 적용 금지
→ HTML/CSS(PC) → @1024 → @768
→ 승인된 preset만 JS/CSS (45 규칙)
→ 섹션 QA — preset ID·과다 적용·중복 확인
→ qa-log · decision-log(템플릿별 매핑 확정 시)
```

**기록 형식 (decision-log / change-log):**

```
[template {slug}] {section-id}: preset — scroll-reveal, hover-tone
```

## 한글명 (명령용)

> 채팅 예: 「story에 **스크롤 등장** 적용해줘」 · 「hero **히어로 슬라이더** 연결해줘」

| 한글명 | ID |
|--------|-----|
| 스크롤 등장 | `scroll-reveal` |
| 호버 톤 | `hover-tone` |
| 이미지 확대 호버 | `image-scale-hover` |
| 흑백→컬러 호버 | `grayscale-hover-color` |
| 가로 드래그 | `drag-scroll` |
| 버튼 글자 슬라이드 | `button-text-slide-hover` |
| 타이핑 연출 | `typing-text` |
| 카드 넓히기 호버 | `expand-card-hover` |
| 숫자 카운트업 | `stats-counter` |
| 히어로 슬라이더 | `hero-progress-slider` |
| GNB 전체 펼침 | `gnb-full-expand` |
| GNB 항목별 패널 | `gnb-item-panel` |

## Interaction Catalog

### `scroll-reveal` · 스크롤 등장

| 항목 | 내용 |
|------|------|
| **한글명** | **스크롤 등장** |
| **목적** | 섹션·블록이 뷰포트에 들어올 때 **은은하게 등장**해 시선 유도 |
| **사용 위치** | story · stats · news · works · faq 등 **섹션 head·카드 그룹** (PC) |
| **동작 설명** | 스크롤로 트리거 영역 진입 시 opacity 0→1 · translateY(소폭) · **1회** 재생 |
| **권장 옵션** | duration 0.5~0.8s · easing ease-out · threshold 10~15% · `prefers-reduced-motion: reduce` 시 **즉시 표시** |
| **사용 금지** | hero 첫 화면(이미 보임) · header · **같은 섹션에 typing-text와 동시** · 전 페이지 모든 블록 일괄(과부하) |

#### 등장 단위 — **프로젝트 기본값** (2026-07-06 확정 · 별도 지시 없으면 필수)

> **카드 = 배경+내용 한 덩어리** · **헤드·본문(카드 아님) = 세부 단위**  
> 참고 구현: `wordpress/365-barun-dental/` · 규칙 본문: `45-interaction-patterns.mdc` 「scroll-reveal」

| 콘텐츠 유형 | `scroll-reveal` 위치 | 금지 |
|-------------|---------------------|------|
| **카드** — 사진 BG · 흰/색 패널 BG · 오버레이·캡션이 **한 프레임**인 블록 | **카드 루트 1개** (`article` / `figure` / `li` 카드 셸 · matrix 패널 · media `figure` 등) — **배경·사진·글자 함께** 등장 | 카드 **안** img · caption · title · `p`에 각각 `scroll-reveal` → **BG만 남고 글자만 뜨는 현상** |
| **섹션 헤드** — label · section title · lead | label · `__title-line` **줄마다** · lead `p` **문장마다** | 헤드 전체를 카드처럼 1덩어리만 (예외는 사용자 승인) |
| **리스트 행** — 카드 BG **없는** 원칙·스텝 설명 등 | 번호 · 제목 · 본문 **각각** | 행 전체 1덩어리만 (카드가 아닌 경우) |
| **hero · header · footer** | **미적용** | — |

- 카드 루트에 `scroll-reveal`이면 **자식에 `scroll-reveal` 없음** (중첩 금지).
- JS: 카드 묶음 **band/batch 일괄 재생 금지** — 요소별 observe + 문서순 큐(약 85ms) (`45`).
- QA: 카드 스크롤 시 **프레임·배경·내용이 동시에** 나타나는지 눈으로 확인 (`50` · `_docs/qa-checklist.md` §4).

---

### `hover-tone` · 호버 톤

| 항목 | 내용 |
|------|------|
| **한글명** | **호버 톤** |
| **목적** | hover 시 **밝기·투명도만** 살짝 바꿔 클릭 가능함을 알림 |
| **사용 위치** | CTA 버튼 · text link · card 링크 (PC, fine pointer) |
| **동작 설명** | `:hover` opacity 약 0.85 또는 배경 명도 ±5% · **transform 없음** |
| **권장 옵션** | transition 0.2~0.25s · `@media (hover: hover) and (pointer: fine)` only |
| **사용 금지** | `image-scale-hover`와 **동일 요소** · 모바일 터치에 hover-only · primary CTA 2개 이상 동시 강조 |

---

### `image-scale-hover` · 이미지 확대 호버

| 항목 | 내용 |
|------|------|
| **한글명** | **이미지 확대 호버** |
| **목적** | 카드·썸네일 hover 시 이미지 **미세 확대** (Fill 프레임 유지) |
| **사용 위치** | story · news · works **이미지 영역** (overflow hidden 프레임 안) |
| **동작 설명** | hover 시 img `scale(1.03~1.08)` · transform-origin center · 프레임 밖은 clip |
| **권장 옵션** | scale **1.05** 기본 · duration 0.35~0.5s · fine pointer only |
| **사용 금지** | **진단용 scale(1.1) 등 임의 수치**를 preset 없이 유지 · grayscale-hover와 **동일 img** · layout shift 유발 |

---

### `grayscale-hover-color` · 흑백→컬러 호버

| 항목 | 내용 |
|------|------|
| **한글명** | **흑백→컬러 호버** |
| **목적** | 기본 **흑백(또는 저채도)** → hover 시 **원색**으로 전환 |
| **사용 위치** | works 갤러리 썸네일 · 로고·파트너 줄 (PC) |
| **동작 설명** | default `grayscale(100%)` or opacity 0.6 → hover `grayscale(0%)` · img 또는 wrapper filter |
| **권장 옵션** | transition 0.3~0.4s · filter only (width/height 변경 금지) |
| **사용 금지** | **image-scale-hover와 동일 요소** · 텍스트 본문 · 접근성 대비 저하 색 |

---

### `drag-scroll` · 가로 드래그

| 항목 | 내용 |
|------|------|
| **한글명** | **가로 드래그** |
| **목적** | 가로로 긴 카드·썸네일 줄을 **마우스 드래그**로 스크롤 |
| **사용 위치** | news 카드 줄 · works · 가로 overflow 리스트 (PC) |
| **동작 설명** | mousedown→mousemove로 scrollLeft 이동 · grab/grabbing cursor · click과 drag 구분 |
| **권장 옵션** | sensitivity 1~1.5 · 관성 optional(짧게) · scrollbar 숨김 시 키보드 포커스 경로 유지 |
| **사용 금지** | **hero-progress-slider와 동일 영역** · 세로 스크롤 페이지와 **가로 drag 충돌** · 모바일(별도 정책 전) |

---

### `button-text-slide-hover` · 버튼 글자 슬라이드

| 항목 | 내용 |
|------|------|
| **한글명** | **버튼 글자 슬라이드** |
| **목적** | 버튼 hover 시 **글자가 슬라이드**하며 hover 카피 또는 동일 텍스트 재진입 |
| **사용 위치** | CTA pill · footer · hero 보조 버튼 (PC) |
| **동작 설명** | 2줄 텍스트 stack · hover 시 translateY(-100%) loop 또는 1회 · pill overflow hidden |
| **권장 옵션** | duration 0.3~0.4s · easing ease · **hover-tone과 병용 가능**(같은 btn) |
| **사용 금지** | **아이콘-only 버튼** · 다줄 문장 버튼 · `expand-card-hover`와 혼동되는 카드 전체 |

---

### `typing-text` · 타이핑 연출

| 항목 | 내용 |
|------|------|
| **한글명** | **타이핑 연출** |
| **목적** | hero·강조 카피 **타이핑 연출** (선택적 1회) |
| **사용 위치** | hero label · 짧은 subtitle (PC, **1블록**) |
| **동작 설명** | 문자 순차 표시 또는 cursor blink · 페이지 로드 **1회** · skip/reduced-motion 시 **전문 즉시 표시** |
| **권장 옵션** | speed 40~80ms/char · max 길이 ~40자 · `prefers-reduced-motion` 필수 대응 |
| **사용 금지** | **scroll-reveal과 동일 head** · body·FAQ·footer · SEO 핵심 문장 전체(긴 문단) |

---

### `expand-card-hover` · 카드 넓히기 호버

| 항목 | 내용 |
|------|------|
| **한글명** | **카드 넓히기 호버** |
| **목적** | hover 시 카드·패널 **너비·flex 비율 확대** (works 아코디언 **유사**) |
| **사용 위치** | works 갤러리 · 가로 카드 그룹 (PC) |
| **동작 설명** | hover(active) 패널 flex 비율 증가 · inactive 축소 · **click 고정** vs **hover preview** — 템플릿별 decision-log에 명시 |
| **권장 옵션** | transition 0.4~0.5s · flex 비율 Figma bbox 기준 · **1개만 expanded** |
| **사용 금지** | **click 아코디언과 hover expand 동시** · FAQ 정적 카드 · 모바일 stack에서 flex expand |

> **mainstream works:** 현재 **click** `is-active` — `expand-card-hover` 적용 시 **hover vs click** 정책 사용자 승인 필수.

---

### `stats-counter` · 숫자 카운트업

| 항목 | 내용 |
|------|------|
| **한글명** | **숫자 카운트업** |
| **목적** | stats 숫자 **0→목표값 카운트업** (스크롤 진입 1회) |
| **사용 위치** | stats 섹션 **수치** (`98%`, `3배` 등) |
| **동작 설명** | Intersection 진입 시 requestAnimationFrame 또는 jQuery animate · **1회** · `%`·접미사 유지 |
| **권장 옵션** | duration 1.2~2s · easing ease-out · reduced-motion 시 **최종값 즉시** |
| **사용 금지** | **scroll-reveal과 동일 숫자 노드에 transform 중복** · non-numeric 카피 · header/footer |

---

### `hero-progress-slider` · 히어로 슬라이더

| 항목 | 내용 |
|------|------|
| **한글명** | **히어로 슬라이더** |
| **목적** | hero **KV 슬라이드 + pager/progress** (Figma 01/03 · progress bar) |
| **사용 위치** | hero 섹션 **전용** |
| **동작 설명** | 자동·수동 전환 · pager 숫자 · track fill width 연동 · **loop optional** |
| **권장 옵션** | interval 5~7s · transition 0.5s fade or slide · pause on hover · **정적 1장만**이면 preset **미적용** |
| **사용 금지** | news·works에 **동일 preset 복제** · autoplay 무한 + **typing-text 동시** · Figma 슬라이드 없는데 추가 |

> **mainstream hero:** 현재 **정적** — 이 preset **미연결**. 연결 시 사용자 승인 + decision-log.

---

## GNB 패턴 (header 2depth · 표준 preset 상한 **미포함**)

> **정의만 이 파일** — nav 정렬(중앙/우측)·열 너비·딤·닫기 지연·열림 duration 등 **시각·수치는 Figma + `decision-log`**  
> **구현:** `45-interaction-patterns.mdc` 「GNB 2depth 패턴」  
> **참고 허브:** `_harness/interaction-samples/samples.manifest.json`

- 템플릿 **header당 GNB 패턴 1개만** — `gnb-full-expand` 와 `gnb-item-panel` **동시 적용 금지**.
- 2depth **자식 1개** 메뉴는 **패널 없이** 1depth 링크 직행 (`decision-log`에 예외 없으면 기본).
- PC **fine pointer** hover 기준 — 모바일 GNB는 `35-responsive.mdc` · 별도 승인.

### `gnb-full-expand` · GNB 전체 펼침

| 항목 | 내용 |
|------|------|
| **한글명** | **GNB 전체 펼침** |
| **목적** | 1depth 호버 시 2depth가 **헤더 아래 공용 패널 1개**로 펼쳐짐 |
| **사용 위치** | header · GNB · **2depth가 있는** PC 메뉴 |
| **동작 설명 (필수)** | ① 2depth = **단일 패널** (헤더 하위·전체 폭) ② 1depth 전환 = 패널을 새로 띄우지 않고 **같은 패널 유지** ③ 1depth 수와 동일한 **열(column) 슬롯**이 패널 안에 미리 존재 (2depth 없는 항목은 **빈 슬롯**) ④ 항목마다 **따로 뜨는 박스 아님** ⑤ **기본=닫힘** · hover·focus 시에만 열림 |
| **참고** | [원안과](https://www.oneretinaclinic.com/) |
| **Figma / decision-log** | nav 정렬(중앙/우측) · 1depth gap · 패널 BG · 딤 유무·opacity · 닫기 지연·외부 클릭 · 열림 duration · **열 정렬 방식**(라벨 기준 등) · `@1024` 모바일 대체 UI |
| **사용 금지** | 1depth마다 **개별 패널** (`gnb-item-panel`과 혼용) · **`[hidden]`인데 CSS로 `display:grid` 등 강제 노출** · 2depth **열 배경 하이라이트 박스**(활성은 1depth 타이포만) · 섹션 `scroll-reveal` 등과 **동일 노드** 중복 motion · 승인·decision-log 없이 적용 |

### `gnb-item-panel` · GNB 항목별 패널

| 항목 | 내용 |
|------|------|
| **한글명** | **GNB 항목별 패널** |
| **목적** | 1depth마다 **해당 항목 아래(또는 근처)에 2depth 패널이 개별**로 열림 |
| **사용 위치** | header · GNB · Figma에 **항목별 드롭다운 박스**가 있는 PC 메뉴 |
| **동작 설명 (필수)** | ① 2depth = **1depth 트리거마다 (또는 공유 1개를 이동하는) 개별 패널** ② 한 시점에 **하나의 패널만** 표시 ③ 패널 위치는 **트리거 기준** (겹침·offset은 Figma) ④ **전체 폭 메가메뉴 1개가 아님** |
| **참고** | Figma `583:54` `GNB / 2depth Dropdown Spec` · **참고 구현** `wordpress/365-barun-dental/` (`initHeaderDropdown`) |
| **Figma / decision-log** | 패널 bbox · 트리거 overlap · 딤 범위(헤더 아래만 등) · 닫기(mouseleave 지연·외부 클릭) · 자식 1개=링크 직행 · 모바일 |
| **사용 금지** | **전체 펼침 단일 패널** (`gnb-full-expand`와 혼용) · 2depth 없는 항목에 빈 패널 · 승인·decision-log 없이 적용 |

**기록 형식 (decision-log):**

```
[template {slug}] header: gnb-pattern — gnb-full-expand
[template {slug}] header: gnb-pattern — gnb-item-panel
```

---

## Preset ID 목록 (고정)

| 한글명 | ID |
|--------|-----|
| 스크롤 등장 | `scroll-reveal` |
| 호버 톤 | `hover-tone` |
| 이미지 확대 호버 | `image-scale-hover` |
| 흑백→컬러 호버 | `grayscale-hover-color` |
| 가로 드래그 | `drag-scroll` |
| 버튼 글자 슬라이드 | `button-text-slide-hover` |
| 타이핑 연출 | `typing-text` |
| 카드 넓히기 호버 | `expand-card-hover` |
| 숫자 카운트업 | `stats-counter` |
| 히어로 슬라이더 | `hero-progress-slider` |

### GNB 패턴 (header · 상한 별도)

| 한글명 | ID |
|--------|-----|
| GNB 전체 펼침 | `gnb-full-expand` |
| GNB 항목별 패널 | `gnb-item-panel` |

## QA (`50-qa-checklist.mdc` 연동)

- [ ] 적용 preset ID = **사용자 승인 목록**과 일치
- [ ] 섹션당 **상한** 준수
- [ ] **중복** animation·transition 없음
- [ ] **`scroll-reveal` 등장 단위** = 기본값 표 준수 (카드 루트 통째 · 헤드·비카드 본문 세부 · 카드 내 자식 `scroll-reveal` 없음)
- [ ] `prefers-reduced-motion` · 768 hover 정책 (`45` · `35`)
- [ ] **GNB 패턴** = decision-log 승인 ID와 일치 · **한 템플릿에 2종 동시 없음**
- [ ] **`gnb-full-expand`:** 기본 닫힘 · hover 시만 전체 폭 패널 · 1depth↔2depth **텍스트 시작점 정렬** · **열 배경 박스 없음**
- [ ] 카탈로그 **미등록** 커스텀 motion 없음

적용 기준:
- 인터랙션 **선택·승인·적용·QA** 시. 카탈로그만 볼 때는 참고.

금지:
- 카탈로그에 없는 preset 이름 invent
- 승인 없이 10종 중 **임의 다수** 섹션 일괄 적용
- **GNB 패턴 2종 동시** · 승인 없는 header 2depth 동작
- `_modules` · 템플릿 간 공유 JS 런타임 생성

완료 기준:
- 승인된 preset만 구현 · QA PASS · 템플릿별 매핑은 `decision-log` (연결 확정 시).
