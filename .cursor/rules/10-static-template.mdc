---
description: 크몽 정적 HTML 템플릿 전용 — templates/{slug} (cafe24_shop·cafe24 제품 제외)
alwaysApply: false
globs:
  - _archive/templates/**
  - _archive/delivery/**
  - _harness/package-delivery.js
  - .cursor/rules/60-delivery.mdc
---

# Static Template Track

> **상태:** **동결** — `_docs/active-track.md` · 신규 정적 진입 금지.  
> **보관:** `_archive/templates/` · 구 납품 `_archive/delivery/`  
> **활성 납품:** WP `wordpress/{slug}/dist/` (`70-wordpress`)

## 스택 · 공간

- HTML + CSS + jQuery 3.x(CDN). 프레임워크·빌드 도구 없음.
- 과거 구현 경로 `templates/{slug}/` → 아카이브됨.

## 납품

- 정적 `package-delivery.js` — **신규 미사용**
- 구 `_delivery/` → `_archive/delivery/` · 루트 재생성 금지

## 금지 (정적 트랙)

- 루트 `templates/` · `_delivery/` 재도입으로 활성 WP 워크플로 혼선
- 승인·QA 없이 완료 선언

카페24 이식은 **Track B** (`81-cafe24-platform-map`)로 넘긴다. 정적 결과만으로 스킨 코딩하지 않는다.
